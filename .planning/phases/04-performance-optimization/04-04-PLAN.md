---
phase: 04-performance-optimization
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - scripts/benchmark_vs_sklearn.py
  - docs/benchmarks.md
autonomous: false
requirements: [PERF-13, PERF-14]

must_haves:
  truths:
    - "Cross-library benchmarks show no regressions from v0.3.1 baseline"
    - "PCA, LinearSVC, OLS/Ridge all within 2-3x of sklearn"
    - "Published benchmark page exists with formatted results and methodology"
  artifacts:
    - path: "scripts/benchmark_vs_sklearn.py"
      provides: "Automated cross-library comparison script"
      contains: "benchmark_vs_sklearn"
    - path: "docs/benchmarks.md"
      provides: "Published benchmark comparison page"
      contains: "Methodology"
  key_links:
    - from: "scripts/benchmark_vs_sklearn.py"
      to: "docs/benchmarks.md"
      via: "Script results formatted into benchmark page"
      pattern: "results"
---

<objective>
Run cross-library performance benchmarks comparing FerroML vs sklearn (PERF-13) and create a published benchmark comparison page (PERF-14).

Purpose: This is the capstone plan that proves all optimizations work. Depends on Plans 01-03 completing stability tests, threshold tuning, and individual benchmarks. This plan runs the full comparison and publishes results.

Output: Cross-library benchmark results, published docs/benchmarks.md with methodology and formatted comparison tables.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-optimization/04-RESEARCH.md
@.planning/phases/04-performance-optimization/04-01-SUMMARY.md
@.planning/phases/04-performance-optimization/04-02-SUMMARY.md
@.planning/phases/04-performance-optimization/04-03-SUMMARY.md

@scripts/benchmark_vs_sklearn.py
@scripts/benchmark_cross_library.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-library performance benchmarks</name>
  <files>scripts/benchmark_vs_sklearn.py</files>
  <action>
Update `scripts/benchmark_vs_sklearn.py` to include comprehensive benchmarks for all PERF requirements. The script should:

1. **Benchmark the following algorithms** (FerroML vs sklearn, same data, same hyperparameters):
   - PCA: fit on 10000x100 matrix, n_components=10 (PERF-01 target: within 2x)
   - TruncatedSVD: fit on 10000x100 matrix, n_components=10 (PERF-02)
   - LDA: fit on 5000x50 matrix, 3 classes, n_components=2 (PERF-03)
   - FactorAnalysis: fit on 5000x50 matrix, n_components=5 (PERF-04)
   - LinearSVC: fit on 5000x50 binary classification (PERF-05 target: within 2x)
   - OLS (LinearRegression): fit on 10000x100 (PERF-07 target: within 2x)
   - Ridge: fit on 10000x100, alpha=1.0 (PERF-08)
   - KMeans: fit on 5000x50, n_clusters=10 (PERF-11)
   - HistGradientBoosting: fit on 10000x20, 50 iterations (PERF-09)
   - SVC (RBF): fit on 2000x20 binary (PERF-10)

2. **For each benchmark:**
   - Generate data with numpy (fixed seed=42 for reproducibility)
   - Warm up with 1 run
   - Time 5 runs, report median
   - Compute ratio: ferroml_time / sklearn_time
   - Flag regressions if ratio > 3.0x (outside 2-3x target)

3. **Output format:**
   - Print a formatted table to stdout
   - Also write results to `docs/benchmark_results.json` for the benchmark page

4. **Prerequisites check:**
   - Script must activate the venv and check that ferroml, sklearn, numpy are importable
   - Must run `maturin develop --release` before benchmarking (or check binary is fresh)

Use the existing `scripts/benchmark_vs_sklearn.py` as the base -- extend it, do not rewrite from scratch.

Run the benchmarks:
```bash
source .venv/bin/activate
maturin develop --release -m ferroml-python/Cargo.toml
python scripts/benchmark_vs_sklearn.py
```

Capture the results for the next task.
  </action>
  <verify>
    <automated>source /home/tlupo/ferroml/.venv/bin/activate && python /home/tlupo/ferroml/scripts/benchmark_vs_sklearn.py 2>&1 | tail -30</automated>
  </verify>
  <done>Cross-library benchmarks run for all 10 algorithms. Results table shows FerroML vs sklearn ratios. No algorithm exceeds 3x slower than sklearn (or regressions documented with explanation).</done>
</task>

<task type="auto">
  <name>Task 2: Published benchmark comparison page</name>
  <files>docs/benchmarks.md</files>
  <action>
Create `docs/benchmarks.md` as the published benchmark comparison page. Structure:

```markdown
# FerroML Performance Benchmarks

## Methodology

- **Hardware**: [auto-detect from `lscpu` or `uname -a`]
- **OS**: [from `uname -srm`]
- **Rust version**: [from `rustc --version`]
- **Python version**: [from `python --version`]
- **sklearn version**: [from `python -c "import sklearn; print(sklearn.__version__)"`]
- **FerroML version**: v0.4.0-dev (commit [short hash])
- **Measurement**: Median of 5 runs after 1 warmup run
- **Data**: Synthetic datasets with fixed seed (numpy random state 42)

## Results

### Linear Algebra / Decomposition

| Algorithm | Dataset | FerroML (ms) | sklearn (ms) | Ratio | Target |
|-----------|---------|--------------|--------------|-------|--------|
| PCA | 10000x100, k=10 | X | Y | X/Y | <= 2.0x |
| TruncatedSVD | 10000x100, k=10 | X | Y | X/Y | <= 2.0x |
| LDA | 5000x50, 3 classes | X | Y | X/Y | <= 2.0x |
| FactorAnalysis | 5000x50, k=5 | X | Y | X/Y | <= 3.0x |

### Linear Models

| Algorithm | Dataset | FerroML (ms) | sklearn (ms) | Ratio | Target |
|-----------|---------|--------------|--------------|-------|--------|
| OLS | 10000x100 | X | Y | X/Y | <= 2.0x |
| Ridge | 10000x100, alpha=1.0 | X | Y | X/Y | <= 2.0x |

### SVM

| Algorithm | Dataset | FerroML (ms) | sklearn (ms) | Ratio | Target |
|-----------|---------|--------------|--------------|-------|--------|
| LinearSVC | 5000x50, binary | X | Y | X/Y | <= 2.0x |
| SVC (RBF) | 2000x20, binary | X | Y | X/Y | <= 3.0x |

### Ensemble / Boosting

| Algorithm | Dataset | FerroML (ms) | sklearn (ms) | Ratio | Target |
|-----------|---------|--------------|--------------|-------|--------|
| HistGBT | 10000x20, 50 iters | X | Y | X/Y | <= 3.0x |

### Clustering

| Algorithm | Dataset | FerroML (ms) | sklearn (ms) | Ratio | Target |
|-----------|---------|--------------|--------------|-------|--------|
| KMeans | 5000x50, k=10 | X | Y | X/Y | <= 2.0x |

## Notes

- FerroML uses faer 0.20 for linear algebra (divide-and-conquer SVD, Cholesky)
- sklearn uses OpenBLAS/MKL (system BLAS) for linear algebra
- Ratios > 1.0x mean FerroML is slower; < 1.0x means FerroML is faster
- [Include any notable findings from Plans 01-03]

## Regression Check

Comparison against v0.3.1 baseline (Plan W benchmarks):
- KMeans: [was 3.4x FASTER, now ...]
- RF: [was 5x FASTER, now ...]
- GaussianNB: [was 4.3x FASTER, now ...]
- StandardScaler: [was 9x FASTER, now ...]
```

Fill in the actual values from the benchmark results in Task 1. Include system information gathered via shell commands. If `docs/benchmark_results.json` was created, parse it to auto-fill the table.
  </action>
  <verify>
    <automated>test -f /home/tlupo/ferroml/docs/benchmarks.md && grep -c "FerroML" /home/tlupo/ferroml/docs/benchmarks.md</automated>
  </verify>
  <done>docs/benchmarks.md exists with complete methodology section, formatted results tables for all 10 algorithms, regression comparison against v0.3.1, and system information. All PERF targets documented with pass/fail status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify benchmark results and performance targets</name>
  <files>docs/benchmarks.md</files>
  <action>
Human reviews the complete benchmark results and performance page.

What was built: Cross-library benchmark suite and published benchmark page covering all 14 PERF requirements. Stability tests, solver benchmarks, threshold tuning, and cross-library comparison all executed.

Verification steps:
1. Review `docs/benchmarks.md` -- confirm all 10 algorithms have results
2. Check that PCA, LinearSVC, and OLS/Ridge are within 2x of sklearn
3. Check that no algorithm regressed vs v0.3.1 baselines
4. Run `cargo test -p ferroml-core --test edge_cases -- stability_tests` -- all pass
5. Optionally re-run `python scripts/benchmark_vs_sklearn.py` to verify reproducibility

Type "approved" to continue or describe issues with benchmark results.
  </action>
  <verify>
    <automated>test -f /home/tlupo/ferroml/docs/benchmarks.md && cargo test -p ferroml-core --test edge_cases -- stability_tests 2>&1 | tail -5</automated>
  </verify>
  <done>Human approved benchmark results. All performance targets met or documented with explanations.</done>
</task>

</tasks>

<verification>
- Cross-library benchmarks complete: `python scripts/benchmark_vs_sklearn.py` exits 0
- Benchmark page exists: `test -f docs/benchmarks.md`
- All stability tests pass: `cargo test --test edge_cases -- stability_tests`
- No test regressions: `cargo test -p ferroml-core`
</verification>

<success_criteria>
1. Cross-library benchmark results for 10 algorithms (FerroML vs sklearn)
2. PCA within 2x of sklearn on 10000x100 (or documented explanation if not)
3. LinearSVC within 2x of sklearn on 5000x50 (or documented explanation)
4. OLS/Ridge within 2x of sklearn on 10000x100 (or documented explanation)
5. No regressions from v0.3.1 baseline
6. Published docs/benchmarks.md with methodology and formatted results
7. Human approval of benchmark results
</success_criteria>

<output>
After completion, create `.planning/phases/04-performance-optimization/04-04-SUMMARY.md`
</output>
