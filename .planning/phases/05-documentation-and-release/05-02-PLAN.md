---
phase: 05-documentation-and-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ferroml-python/src/linear.rs
  - ferroml-python/src/preprocessing.rs
  - ferroml-python/src/decomposition.rs
  - ferroml-python/src/neighbors.rs
  - ferroml-python/src/trees.rs
  - ferroml-python/src/clustering.rs
  - ferroml-python/src/neural.rs
  - ferroml-python/src/multioutput.rs
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-04]

must_haves:
  truths:
    - "help(RobustRegression) shows Parameters with type/default/range, Attributes, and runnable Example"
    - "help(PowerTransformer) shows Parameters, Attributes, and runnable Example"
    - "help(RandomForestClassifier) shows Notes about parallel non-determinism"
    - "help(HistGradientBoostingClassifier) shows Notes about NaN handling behavior"
    - "help(IncrementalPCA) shows Parameters, Attributes, and runnable Example"
    - "help(StandardScaler) existing docstring verified correct against actual defaults"
  artifacts:
    - path: "ferroml-python/src/linear.rs"
      provides: "Complete docstrings for 8 missing models + 5 audited"
      contains: "/// Examples"
    - path: "ferroml-python/src/preprocessing.rs"
      provides: "Complete docstrings for 12 missing models + 14 audited"
      contains: "/// Examples"
    - path: "ferroml-python/src/decomposition.rs"
      provides: "Complete docstrings for 5 missing + 2 audited"
      contains: "/// Examples"
    - path: "ferroml-python/src/trees.rs"
      provides: "Audited docstrings for 8 models with Notes added"
      contains: "/// Notes"
    - path: "ferroml-python/src/clustering.rs"
      provides: "Audited docstrings for 5 models"
      contains: "/// Examples"
  key_links:
    - from: "ferroml-python/src/trees.rs"
      to: "ferroml.trees"
      via: "PyO3 doc-comments compiled into __doc__"
      pattern: "/// Notes"
---

<objective>
Complete docstrings for binding files that have PARTIAL coverage (some models already have Examples) and AUDIT files that already have full coverage. Add missing Examples, fill in Parameters gaps, and add Notes sections where applicable.

Files: linear.rs (8 missing + 5 audit), preprocessing.rs (12 missing + 14 audit), decomposition.rs (5 missing + 2 audit), neighbors.rs (1 missing + 2 audit), trees.rs (8 audit), clustering.rs (5 audit), neural.rs (2 audit), multioutput.rs (2 audit). Total: ~66 models.

Purpose: Complete DOCS-01 (all models documented), DOCS-02 (all parameters documented), DOCS-04 (per-model limitations).

Output: 8 Rust binding files with complete, audited docstrings.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-and-release/05-CONTEXT.md
@.planning/phases/05-documentation-and-release/05-RESEARCH.md

Reference files for docstring template:
@ferroml-python/src/linear.rs (lines 52-90 for LinearRegression gold-standard template)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docstrings for linear.rs, decomposition.rs, neighbors.rs</name>
  <files>ferroml-python/src/linear.rs, ferroml-python/src/decomposition.rs, ferroml-python/src/neighbors.rs</files>
  <action>
**linear.rs -- 8 models need Examples added, 5 need audit:**

Models needing full docstrings: RobustRegression, QuantileRegression, Perceptron, RidgeCV, LassoCV, ElasticNetCV, RidgeClassifier, IsotonicRegression.

Models needing audit only (already have Examples): LinearRegression, Ridge, Lasso, ElasticNet, OLS. For audit-only models: verify documented defaults match `#[new]` implementation, verify type annotations are present, verify valid ranges are stated.

For each model needing docstrings:
1. Read `#[new]` constructor for actual parameter defaults
2. Read `#[pymethods]` for exposed attributes
3. Write complete docstring: description, Parameters (type + optional + default + valid range), Attributes (type + shape), Examples (runnable: import, data, fit, predict)
4. Highlight statistical diagnostics where available (e.g., summary() on linear models, confidence intervals)

**decomposition.rs -- 5 models need Examples, 2 need audit:**

Models needing docstrings: IncrementalPCA, TruncatedSVD, LDA, FactorAnalysis, and check for others.
Models needing audit: PCA, NMF (verify existing docstrings).

**neighbors.rs -- 1 model needs Examples, 2 need audit:**

Model needing docstring: NearestCentroid.
Models needing audit: KNeighborsClassifier, KNeighborsRegressor.

Same format as Plan 01: NumPy-style docstrings matching LinearRegression template.
  </action>
  <verify>
    <automated>cd /home/tlupo/ferroml && grep -c "/// Examples" ferroml-python/src/linear.rs ferroml-python/src/decomposition.rs ferroml-python/src/neighbors.rs</automated>
  </verify>
  <done>All 13 linear models, all 7 decomposition models, and all 3 neighbors models have complete docstrings. Existing docstrings audited for parameter accuracy. Linear models highlight summary() and confidence intervals.</done>
</task>

<task type="auto">
  <name>Task 2: Docstrings for preprocessing.rs + audit trees, clustering, neural, multioutput</name>
  <files>ferroml-python/src/preprocessing.rs, ferroml-python/src/trees.rs, ferroml-python/src/clustering.rs, ferroml-python/src/neural.rs, ferroml-python/src/multioutput.rs</files>
  <action>
**preprocessing.rs -- 12 models need Examples, 14 need audit:**

Models needing full docstrings: PowerTransformer, QuantileTransformer, PolynomialFeatures, KBinsDiscretizer, VarianceThreshold, SelectKBest, KNNImputer, TargetEncoder, ADASYN, RandomUnderSampler, RandomOverSampler, Normalizer.

Models needing audit: StandardScaler, MinMaxScaler, MaxAbsScaler, RobustScaler, LabelEncoder, OrdinalEncoder, OneHotEncoder, SimpleImputer, CountVectorizer, TfidfVectorizer, TfidfTransformer, SMOTE, Binarizer, FunctionTransformer.

For audit-only models: verify defaults match `#[new]`, verify type+default+range format is consistent with template, fix any "default: X" to "optional (default=X)" format.

**trees.rs -- 8 models, audit only (all have Examples):**

Audit: RandomForestClassifier, RandomForestRegressor, DecisionTreeClassifier, DecisionTreeRegressor, GradientBoostingClassifier, GradientBoostingRegressor, HistGradientBoostingClassifier, HistGradientBoostingRegressor.

Add Notes sections per user decisions:
- RandomForest models: "Results may vary between runs due to Rayon work-stealing parallelism. Set n_jobs=1 for reproducibility."
- HistGBT models: "Handles NaN values natively. Missing values are routed to the optimal split direction during training."

**clustering.rs -- 5 models, audit only:**
Audit: KMeans, DBSCAN, HDBSCAN, AgglomerativeClustering, GaussianMixture. Verify docstrings are complete.

**neural.rs -- 2 models, audit only:**
Audit: MLPClassifier, MLPRegressor. Verify docstrings.

**multioutput.rs -- 2 models, audit only:**
Audit: MultiOutputClassifier, MultiOutputRegressor. Verify docstrings.

Same NumPy-style format throughout. Run `cargo fmt --all` after all edits.
  </action>
  <verify>
    <automated>cd /home/tlupo/ferroml && grep -c "/// Examples" ferroml-python/src/preprocessing.rs ferroml-python/src/trees.rs ferroml-python/src/clustering.rs ferroml-python/src/neural.rs ferroml-python/src/multioutput.rs</automated>
  </verify>
  <done>All 26 preprocessing models, 8 tree models, 5 clustering models, 2 neural models, and 2 multioutput models have complete docstrings. RF models have Notes about parallel non-determinism. HistGBT models have Notes about NaN handling. All existing docstrings audited for consistency.</done>
</task>

</tasks>

<verification>
After both tasks, verify:
- linear.rs: >= 13 "/// Examples" (all 13 models)
- preprocessing.rs: >= 26 "/// Examples" (all 26 models)
- decomposition.rs: >= 7 "/// Examples"
- trees.rs: >= 8 "/// Examples" + >= 4 "/// Notes" (RF + HistGBT)
- clustering.rs: >= 5 "/// Examples"
- neighbors.rs: >= 3 "/// Examples"
- neural.rs: >= 2 "/// Examples"
- multioutput.rs: >= 2 "/// Examples"

Run `cargo fmt --all` to ensure formatting passes pre-commit hook.
</verification>

<success_criteria>
- 66 classes across 8 files have complete, audited NumPy-style docstrings
- All 8 models missing Examples in linear.rs now have them
- All 12 models missing Examples in preprocessing.rs now have them
- RF models have Notes about parallel non-determinism
- HistGBT models have Notes about NaN handling
- All audited models have consistent format: type + optional + default + valid range
- `cargo fmt --all` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-and-release/05-02-SUMMARY.md`
</output>
