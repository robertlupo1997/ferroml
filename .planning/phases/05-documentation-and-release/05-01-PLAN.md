---
phase: 05-documentation-and-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferroml-python/src/ensemble.rs
  - ferroml-python/src/cv.rs
  - ferroml-python/src/gaussian_process.rs
  - ferroml-python/src/svm.rs
  - ferroml-python/src/naive_bayes.rs
  - ferroml-python/src/anomaly.rs
  - ferroml-python/src/calibration.rs
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-04]

must_haves:
  truths:
    - "help(SVC) shows Parameters with type/default/range, Attributes, Examples, and Notes about scaling sensitivity"
    - "help(GaussianNB) shows Parameters, Attributes, and a runnable Example"
    - "help(AdaBoostClassifier) shows Parameters, Attributes, and a runnable Example"
    - "help(KFold) shows Parameters and a runnable Example"
    - "help(GaussianProcessRegressor) shows Parameters, Attributes, Examples, and Notes about no pickle support"
    - "help(IsolationForest) shows Parameters, Attributes, and a runnable Example"
  artifacts:
    - path: "ferroml-python/src/svm.rs"
      provides: "Docstrings for SVC, SVR, LinearSVC, LinearSVR"
      contains: "/// Examples"
    - path: "ferroml-python/src/naive_bayes.rs"
      provides: "Docstrings for GaussianNB, MultinomialNB, BernoulliNB, CategoricalNB"
      contains: "/// Examples"
    - path: "ferroml-python/src/ensemble.rs"
      provides: "Docstrings for all 13 ensemble models"
      contains: "/// Examples"
    - path: "ferroml-python/src/cv.rs"
      provides: "Docstrings for all 8 CV splitters"
      contains: "/// Examples"
    - path: "ferroml-python/src/gaussian_process.rs"
      provides: "Docstrings for GP models and kernels"
      contains: "/// Examples"
    - path: "ferroml-python/src/anomaly.rs"
      provides: "Docstrings for IsolationForest, LocalOutlierFactor"
      contains: "/// Examples"
    - path: "ferroml-python/src/calibration.rs"
      provides: "Docstring for TemperatureScalingCalibrator"
      contains: "/// Examples"
  key_links:
    - from: "ferroml-python/src/svm.rs"
      to: "ferroml.svm"
      via: "PyO3 doc-comments compiled into __doc__"
      pattern: "/// Notes"
---

<objective>
Add complete NumPy-style docstrings to all models in binding files that currently have NO examples: ensemble (13 models), cv (8 splitters), gaussian_process (9 classes), svm (4 models), naive_bayes (4 models), anomaly (2 models), calibration (1 model). Total: 41 classes needing full docstrings.

Purpose: DOCS-01 and DOCS-02 require all 55+ models to have complete docstrings with description, parameters (type + default + range), attributes, and runnable examples. DOCS-04 requires per-model limitation notes where applicable.

Output: 7 Rust binding files updated with complete docstrings following the LinearRegression gold-standard template.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-and-release/05-CONTEXT.md
@.planning/phases/05-documentation-and-release/05-RESEARCH.md

Reference files for docstring template:
@ferroml-python/src/linear.rs (lines 52-90 for LinearRegression gold-standard template)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docstrings for ensemble.rs, cv.rs, gaussian_process.rs</name>
  <files>ferroml-python/src/ensemble.rs, ferroml-python/src/cv.rs, ferroml-python/src/gaussian_process.rs</files>
  <action>
Add complete NumPy-style docstrings (as `///` Rust doc-comments above each `#[pyclass]`) for every model in these 3 files. Follow the LinearRegression template from linear.rs lines 52-90.

**ensemble.rs (13 models):** ExtraTreesClassifier, ExtraTreesRegressor, AdaBoostClassifier, AdaBoostRegressor, SGDClassifier, SGDRegressor, PassiveAggressiveClassifier, BaggingClassifier, BaggingRegressor, VotingClassifier, VotingRegressor, StackingClassifier, StackingRegressor.

For each model:
1. Read the `#[new]` constructor to find all parameters with their actual default values
2. Read the `#[pymethods]` impl block to find fit/predict return types and exposed attributes
3. Write docstring with sections: description (mention FerroML differentiators where applicable), Parameters (type + optional + default + valid range), Attributes (type + shape), Examples (5-8 lines: import, data, fit, predict/transform), Notes (only if model has user-facing gotchas)

**cv.rs (8 splitters):** KFold, StratifiedKFold, RepeatedKFold, RepeatedStratifiedKFold, LeaveOneOut, LeavePOut, ShuffleSplit, StratifiedShuffleSplit.

For CV splitters: description, Parameters, Examples showing `split(X, y)` usage. No Attributes section needed (splitters don't have fitted attributes). Brief format since these are utility classes.

**gaussian_process.rs (9 classes):** RBFKernel, MaternKernel, RationalQuadraticKernel, DotProductKernel (4 kernels), GaussianProcessRegressor, GaussianProcessClassifier, SparseGPR, SparseGPC, SVGP.

For kernels: brief Parameters-only docstring (no Examples -- kernels are passed to GP models).
For GP models: full docstring with Parameters, Attributes, Examples. Add Notes section: "GP models do not support pickle serialization due to kernel trait objects."

**Format for all doc-comments:**
```rust
/// One-line summary.
///
/// Extended description.
///
/// Parameters
/// ----------
/// param : type, optional (default=value)
///     Description. Valid range: [min, max].
///
/// Attributes
/// ----------
/// attr : type
///     Description.
///
/// Examples
/// --------
/// >>> from ferroml.module import Model
/// >>> import numpy as np
/// >>> X = np.array(...)
/// >>> model = Model()
/// >>> model.fit(X, y)
/// >>> model.predict(X)
///
/// Notes
/// -----
/// - Only if applicable.
#[pyclass(...)]
```

Cross-reference each parameter's default value with the actual Rust `#[new]` implementation. Do NOT guess defaults.
  </action>
  <verify>
    <automated>cd /home/tlupo/ferroml && grep -c "/// Examples" ferroml-python/src/ensemble.rs ferroml-python/src/cv.rs ferroml-python/src/gaussian_process.rs</automated>
  </verify>
  <done>All 13 ensemble models, 8 CV splitters, and 9 GP classes have complete docstrings with Parameters sections. GP models and kernels have Notes about pickle limitation.</done>
</task>

<task type="auto">
  <name>Task 2: Docstrings for svm.rs, naive_bayes.rs, anomaly.rs, calibration.rs</name>
  <files>ferroml-python/src/svm.rs, ferroml-python/src/naive_bayes.rs, ferroml-python/src/anomaly.rs, ferroml-python/src/calibration.rs</files>
  <action>
Add complete NumPy-style docstrings for every model in these 4 files using the same approach as Task 1.

**svm.rs (4 models):** SVC, SVR, LinearSVC, LinearSVR.

SVC Notes section MUST include (per user decision):
- Feature scaling strongly recommended (RBF kernel is sensitive to magnitudes)
- For datasets > 3000 samples, consider LinearSVC for better scaling
- Use StandardScaler before SVC

LinearSVC Notes: mention shrinking optimization for large datasets.

**naive_bayes.rs (4 models):** GaussianNB, MultinomialNB, BernoulliNB, CategoricalNB.

**anomaly.rs (2 models):** IsolationForest, LocalOutlierFactor.

**calibration.rs (1 model):** TemperatureScalingCalibrator.

Same process: read `#[new]` for actual defaults, read `#[pymethods]` for attributes, write full docstring. Highlight FerroML differentiators (e.g., decision_function() on SVM, statistical diagnostics where available).
  </action>
  <verify>
    <automated>cd /home/tlupo/ferroml && grep -c "/// Examples" ferroml-python/src/svm.rs ferroml-python/src/naive_bayes.rs ferroml-python/src/anomaly.rs ferroml-python/src/calibration.rs</automated>
  </verify>
  <done>All 4 SVM models have docstrings with SVC Notes about scaling sensitivity. All 4 NB models, 2 anomaly models, and calibration model have complete docstrings with Parameters, Attributes, Examples.</done>
</task>

</tasks>

<verification>
After both tasks, verify docstring counts per file:
- ensemble.rs: >= 13 "/// Examples" occurrences
- cv.rs: >= 8 "/// Examples" occurrences
- gaussian_process.rs: >= 5 "/// Examples" occurrences (kernels may skip Examples)
- svm.rs: >= 4 "/// Examples" occurrences
- naive_bayes.rs: >= 4 "/// Examples" occurrences
- anomaly.rs: >= 2 "/// Examples" occurrences
- calibration.rs: >= 1 "/// Examples" occurrences

Run `cargo fmt --all` to ensure formatting passes pre-commit hook.
</verification>

<success_criteria>
- 41 classes across 7 files have complete NumPy-style docstrings
- Every Parameters section includes type, optional marker, default value, and valid range
- SVC has Notes about scaling sensitivity per user decision
- GP models have Notes about no pickle support per user decision
- All Examples use correct import paths (ferroml.svm, ferroml.ensemble, etc.)
- `cargo fmt --all` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-and-release/05-01-SUMMARY.md`
</output>
