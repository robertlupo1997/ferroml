---
phase: 03-robustness-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferroml-core/src/models/svm.rs
  - ferroml-core/src/stats/hypothesis.rs
  - ferroml-core/src/models/boosting.rs
  - ferroml-core/src/models/hist_boosting.rs
autonomous: true
requirements: [ROBU-01, ROBU-02, ROBU-03]

must_haves:
  truths:
    - "SVM fit/predict cannot panic on validated user input"
    - "Stats module fit/predict cannot panic on validated user input"
    - "Boosting model fit/predict cannot panic on validated user input"
  artifacts:
    - path: "ferroml-core/src/models/svm.rs"
      provides: "SVM with unwraps replaced in fit/predict/decision_function paths"
      contains: "ok_or_else"
    - path: "ferroml-core/src/models/boosting.rs"
      provides: "GradientBoosting with unwraps replaced in fit/predict paths"
      contains: "ok_or_else"
    - path: "ferroml-core/src/models/hist_boosting.rs"
      provides: "HistGBT with unwraps replaced in fit/predict paths"
      contains: "ok_or_else"
    - path: "ferroml-core/src/stats/hypothesis.rs"
      provides: "Stats with unwraps replaced"
      contains: "ok_or_else"
  key_links:
    - from: "ferroml-core/src/models/svm.rs"
      to: "ferroml-core/src/error.rs"
      via: "FerroError::not_fitted and FerroError::numerical"
      pattern: "FerroError::not_fitted|FerroError::NumericalError"
---

<objective>
Replace unsafe unwrap()/expect() calls in fit/predict/transform paths of SVM, stats, and boosting modules with proper error handling.

Purpose: SVM (178 unwraps), boosting (168 combined), and stats (37) are the highest-risk modules. Eliminating panics in these critical paths ensures users get proper errors instead of crashes on edge cases.

Output: Modified svm.rs, boosting.rs, hist_boosting.rs, hypothesis.rs with Tier 1-2 unwraps replaced by proper error propagation.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-robustness-hardening/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage and fix SVM unwraps (Tier 1-2)</name>
  <files>ferroml-core/src/models/svm.rs</files>
  <action>
Triage all ~178 unwrap/expect calls in svm.rs by risk tier using the classification from RESEARCH.md:

**Tier 1 (Critical) -- FIX:** Replace `.as_ref().unwrap()` on Option fields in predict/decision_function paths with `.ok_or_else(|| FerroError::not_fitted("predict"))`. Common patterns:
- `self.support_vectors.as_ref().unwrap()` -> `ok_or_else`
- `self.classes.as_ref().unwrap()` -> `ok_or_else`
- `self.n_features.unwrap()` -> `ok_or_else`

**Tier 2 (Medium) -- FIX:** Replace `.as_slice().unwrap()` on arrays that might be non-contiguous with `.as_slice().ok_or_else(|| FerroError::NumericalError(...))` or iterator fallback.

**Tier 3 (Low) -- DOCUMENT:** For `from_shape_vec(...).unwrap()` where shape is provably correct, add `// SAFETY: shape matches data length` comment. For mutex locks, use `.lock().unwrap_or_else(|e| e.into_inner())`.

Do NOT modify test code or doc comments. Run `cargo fmt --all` after changes.
  </action>
  <verify>
    <automated>cd /home/tlupo/ferroml && cargo test -p ferroml-core --lib models::svm -- --test-threads=4</automated>
  </verify>
  <done>All Tier 1-2 unwraps in svm.rs predict/decision_function/fit paths replaced with proper error handling. Tier 3 unwraps documented with SAFETY comments. All existing SVM tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix boosting and stats module unwraps (Tier 1-2)</name>
  <files>ferroml-core/src/models/boosting.rs, ferroml-core/src/models/hist_boosting.rs, ferroml-core/src/stats/hypothesis.rs</files>
  <action>
Apply the same triage approach to boosting (72 + 96 = 168 unwraps) and stats (37 unwraps):

**boosting.rs (~72 unwraps):**
- Replace `.as_ref().unwrap()` on fitted state (trees, feature_importances, etc.) with `ok_or_else(|| FerroError::not_fitted(...))`
- Replace `from_shape_vec().unwrap()` with either SAFETY comments (proven shapes) or `map_err` (uncertain shapes)
- Check for `.get(i).unwrap()` index access in tree traversal -- use `get().ok_or_else()` if index comes from user data

**hist_boosting.rs (~96 unwraps):**
- Same patterns as boosting.rs -- focus on predict path fitted-state access
- Histogram bin lookups via `.unwrap()` -- add bounds checks or SAFETY comments
- `from_shape_vec` on histogram arrays -- document shape correctness

**stats/hypothesis.rs (~37 unwraps):**
- Mostly safe patterns (low risk). Document Tier 3 with SAFETY comments.
- Fix any `.as_ref().unwrap()` on computed statistical results that could be None on degenerate input.

Run `cargo fmt --all` after changes.
  </action>
  <verify>
    <automated>cd /home/tlupo/ferroml && cargo test -p ferroml-core --lib models::boosting models::hist_boosting stats -- --test-threads=4</automated>
  </verify>
  <done>All Tier 1-2 unwraps in boosting.rs, hist_boosting.rs, and hypothesis.rs replaced with proper error handling. Tier 3 documented. All existing tests pass.</done>
</task>

</tasks>

<verification>
After both tasks: `cd /home/tlupo/ferroml && cargo test -p ferroml-core --lib -- --test-threads=4` (full lib tests pass)
</verification>

<success_criteria>
- Zero Tier 1 (predict-path NotFitted) unwraps remain in svm.rs, boosting.rs, hist_boosting.rs
- Zero Tier 2 (as_slice on views) unwraps remain in these files
- All ~3,550 Rust tests still pass
- No new clippy warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-robustness-hardening/03-01-SUMMARY.md`
</output>
