---
phase: 03-robustness-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ferroml-core/src/models/linear.rs
  - ferroml-core/src/models/regularized.rs
  - ferroml-core/src/models/logistic.rs
  - ferroml-core/src/models/tree.rs
  - ferroml-core/src/models/forest.rs
  - ferroml-core/src/models/extra_trees.rs
  - ferroml-core/src/preprocessing/scalers.rs
  - ferroml-core/src/preprocessing/selection.rs
  - ferroml-core/src/preprocessing/sampling.rs
  - ferroml-core/src/preprocessing/encoders.rs
  - ferroml-core/src/preprocessing/imputers.rs
  - ferroml-core/src/clustering/
  - ferroml-core/src/lib.rs
autonomous: true
requirements: [ROBU-04, ROBU-05, ROBU-06, ROBU-07, ROBU-08, ROBU-09]

must_haves:
  truths:
    - "Linear model fit/predict cannot panic on validated user input"
    - "Tree model fit/predict cannot panic on validated user input"
    - "Preprocessing transform cannot panic on validated user input"
    - "Clustering fit/predict cannot panic on validated user input"
    - "clippy::unwrap_used lint is enabled at warning level"
  artifacts:
    - path: "ferroml-core/src/models/linear.rs"
      provides: "Linear models with unwraps replaced"
      contains: "ok_or_else"
    - path: "ferroml-core/src/models/tree.rs"
      provides: "Tree models with unwraps replaced"
      contains: "ok_or_else"
    - path: "ferroml-core/src/lib.rs"
      provides: "clippy::unwrap_used lint enabled"
      contains: "clippy::unwrap_used"
  key_links:
    - from: "ferroml-core/src/lib.rs"
      to: "all modules"
      via: "warn(clippy::unwrap_used) lint"
      pattern: "warn.*clippy::unwrap_used"
---

<objective>
Replace unsafe unwrap()/expect() calls in linear, tree, preprocessing, and clustering modules, then enable clippy::unwrap_used lint as a workspace warning.

Purpose: These modules have ~1,500+ combined unwraps across linear (203), tree (179), preprocessing (660), and clustering (287). Fixing Tier 1-2 in these plus enabling the clippy lint ensures ongoing protection against new unwrap introduction.

Output: Modified model/preprocessing/clustering files with Tier 1-2 unwraps fixed, lib.rs with clippy lint enabled, module-level #[allow] on not-yet-triaged modules.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-robustness-hardening/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix unwraps in linear, tree, preprocessing, and clustering modules</name>
  <files>ferroml-core/src/models/linear.rs, ferroml-core/src/models/regularized.rs, ferroml-core/src/models/logistic.rs, ferroml-core/src/models/tree.rs, ferroml-core/src/models/forest.rs, ferroml-core/src/models/extra_trees.rs, ferroml-core/src/preprocessing/scalers.rs, ferroml-core/src/preprocessing/selection.rs, ferroml-core/src/preprocessing/sampling.rs, ferroml-core/src/preprocessing/encoders.rs, ferroml-core/src/preprocessing/imputers.rs, ferroml-core/src/clustering/</files>
  <action>
Apply the same tier-based triage as Plan 01 to the remaining core modules. Focus on fit/predict/transform paths only -- do not touch test code or doc comments.

**Linear models (linear.rs 58, regularized.rs 82, logistic.rs 63):**
- Replace `.as_ref().unwrap()` on fitted coefficients/intercept with `ok_or_else(|| FerroError::not_fitted(...))`
- `from_shape_vec().unwrap()` on coefficient matrices -- SAFETY comments (shapes proven from matrix math)
- `as_slice().unwrap()` -- check if on standard-layout arrays (safe) or views (fix)

**Tree models (tree.rs 92, forest.rs 47, extra_trees.rs 40):**
- Replace fitted-state access unwraps in predict/predict_proba paths
- Tree traversal `.get(node_idx).unwrap()` -- these are internally-built indices, use `expect("valid node index")` with SAFETY comment
- `from_shape_vec` on prediction arrays -- SAFETY comments

**Preprocessing (scalers 86, selection 81, sampling 67, encoders 66, imputers 61):**
- Replace `.as_ref().unwrap()` on fitted state (mean_, scale_, categories_) with `ok_or_else(|| FerroError::not_fitted(...))`
- `from_shape_vec` on transform output -- SAFETY comments
- Index access in encoder mapping -- `get().ok_or_else()` where category could be unknown

**Clustering (287 total):**
- Replace fitted-state access (centroids_, labels_) with `ok_or_else`
- `from_shape_vec` on distance/assignment matrices -- SAFETY comments
- DBSCAN/HDBSCAN internal graph operations -- document Tier 3 safety

Run `cargo fmt --all` after changes.
  </action>
  <verify>
    <automated>cd /home/tlupo/ferroml && cargo test -p ferroml-core --lib models::linear models::regularized models::logistic models::tree models::forest preprocessing clustering -- --test-threads=4</automated>
  </verify>
  <done>All Tier 1-2 unwraps in linear, tree, preprocessing, and clustering modules replaced with proper error handling. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Enable clippy::unwrap_used lint and triage remaining modules</name>
  <files>ferroml-core/src/lib.rs</files>
  <action>
1. Add `#![warn(clippy::unwrap_used)]` at the top of `ferroml-core/src/lib.rs`.

2. Add `#[allow(clippy::unwrap_used)]` at the module level for modules NOT yet triaged in Plans 01-02. These are the "remaining modules" from ROBU-08:
   - decomposition (243 unwraps -- medium risk, mostly internal math)
   - onnx (237 -- inference runtime, not user-facing fit/predict)
   - cv (185 -- cross-validation orchestration)
   - explainability (173 -- SHAP/feature importance)
   - datasets (168 -- data loading utilities)
   - ensemble (113 -- VotingClassifier, Stacking)
   - Any other module that triggers lint warnings

3. For the modules already triaged (svm, boosting, hist_boosting, stats, linear, regularized, logistic, tree, forest, extra_trees, preprocessing/, clustering/), do NOT add #[allow]. They should be clean from Task 1 and Plan 01.

4. Run `cargo clippy -p ferroml-core --all-features -- -W clippy::unwrap_used` to verify no new warnings from triaged modules. Remaining warnings should only come from #[allow]'d modules.

5. If any Tier 1-2 unwraps remain in triaged modules (caught by clippy), fix them. For Tier 3 (provably safe), add `#[allow(clippy::unwrap_used)]` at the statement level with a SAFETY comment.

Run `cargo fmt --all` after changes.
  </action>
  <verify>
    <automated>cd /home/tlupo/ferroml && cargo clippy -p ferroml-core --all-features 2>&1 | grep -c "unwrap_used" | xargs -I{} test {} -eq 0 && cargo test -p ferroml-core --lib -- --test-threads=4</automated>
  </verify>
  <done>clippy::unwrap_used is enabled as warning in lib.rs. Triaged modules produce zero clippy warnings. Un-triaged modules have module-level #[allow]. All tests pass.</done>
</task>

</tasks>

<verification>
After both tasks: `cd /home/tlupo/ferroml && cargo test -p ferroml-core --lib -- --test-threads=4 && cargo clippy -p ferroml-core --all-features`
</verification>

<success_criteria>
- Zero Tier 1-2 unwraps remain in linear, tree, preprocessing, clustering modules
- clippy::unwrap_used lint is active at warn level in lib.rs
- No clippy warnings from triaged modules
- All ~3,550 Rust tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-robustness-hardening/03-02-SUMMARY.md`
</output>
